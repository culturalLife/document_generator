MISTRAL_API_KEY=your_mistral_api_key_here

# Optional: override the Mistral HTTP read timeout (seconds). Default is 300 (5 min).
# Increase if mistral-large-latest calls are timing out for large payloads.
# MISTRAL_READ_TIMEOUT=300
