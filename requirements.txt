# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt — VM-Compatible Backend (docs_gen pipeline)
# ──────────────────────────────────────────────────────────────────────────────
# Install on VM:  pip install -r requirements.txt
#
# Versions are pinned to lower bounds (>=) for flexibility.
# For a fully reproducible deploy, pin exact versions after testing:
#   pip freeze > requirements.lock.txt
# ──────────────────────────────────────────────────────────────────────────────

# Web framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Required for FastAPI Form() fields (multipart/form-data parsing)
python-multipart>=0.0.9

# Mistral AI SDK
mistralai>=1.0.0

# Word document generation
python-docx>=1.1.2

# Environment variable loading from .env file
python-dotenv>=1.0.1

# Data validation (FastAPI uses this internally)
pydantic>=2.7.0
